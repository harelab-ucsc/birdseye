[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "birdseye"
version = "0.1.0"
description = "Birdseye camera system"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
